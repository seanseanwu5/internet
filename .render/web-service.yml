services:
  - type: web
    web:
    command: gunicorn --worker-class sync server:app
    name: flask-socketio-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    routes:
      - type: manual
        path: /
