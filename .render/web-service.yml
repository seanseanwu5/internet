services:
  - type: web
    name: flask-socketio-app
    env:
      PYTHON_VERSION: "3.10"
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    routes:
      - type: manual
        path: /
