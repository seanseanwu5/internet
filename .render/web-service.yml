services:
  - type: web
    name: flask-socketio-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    routes:
      - type: manual
        path: /
